# CodexMonitor environment template
# Copy to `.env` (or export in terminal env) and adjust as needed.
# Sensitive values must be sourced from `.env` or terminal env and must NOT be committed.

# Optional Sentry DSN for frontend telemetry. Leave empty to use the in-app default.
VITE_SENTRY_DSN=

# Local Vite dev server host/ports used by Tauri dev.
# TAURI_DEV_HOST is optional; set it when using remote device/browser access.
TAURI_DEV_HOST=
TAURI_DEV_PORT=17420
TAURI_DEV_HMR_PORT=17421

# Playwright local E2E target.
PLAYWRIGHT_WEB_PORT=17473
# Optional override for connecting to an already-running app.
# PLAYWRIGHT_BASE_URL=http://127.0.0.1:17473

# Optional real external integration target for Playwright.
# This is used by `npm run test:e2e:external` and is intentionally non-blocking.
# Example: REAL_EXTERNAL_URL=https://example.com
REAL_EXTERNAL_URL=

# Gemini-only default LLM template (safe placeholders only; do not commit real keys).
GEMINI_API_KEY=
# Compatibility alias for legacy scripts. Keep empty unless needed, or set to same value as GEMINI_API_KEY.
REAL_LLM_API_KEY=
# Recommended Gemini OpenAI-compatible base URL placeholder.
REAL_LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
REAL_LLM_MODEL=gemini-3.1-pro-preview
REAL_LLM_TIMEOUT_MS=45000

# Optional Gemini alias vars (also recognized by real LLM smoke script).
GEMINI_BASE_URL=
GEMINI_MODEL=
GEMINI_TIMEOUT_MS=
